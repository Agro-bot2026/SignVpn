name: Build SingVPN HC APK
on:
  push: { branches: [main, master] }
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { path: signvpn }

      - uses: actions/setup-go@v5
        with: { go-version: '1.25' }

      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '17' }

      - uses: android-actions/setup-android@v3

      - name: Install Android SDK
        run: yes | sdkmanager "platforms;android-34" "build-tools;34.0.0"

      - name: Setup gomobile
        run: |
          cd ${{ github.workspace }}/signvpn
          go install golang.org/x/mobile/cmd/gomobile@latest
          go install golang.org/x/mobile/cmd/gobind@latest
          gomobile init

      - name: Build tunnel AAR
        run: |
          cd ${{ github.workspace }}/signvpn/tunnel
          go get -tool golang.org/x/mobile/cmd/gobind
          go mod tidy
          cd ${{ github.workspace }}/signvpn
          gomobile bind -v -target=android/arm64 -androidapi 21 \
            -o android/app/libs/tunnel.aar ./tunnel

      - name: Setup Gradle
        run: |
          cd ${{ github.workspace }}/signvpn/android
          curl -sL https://services.gradle.org/distributions/gradle-8.5-bin.zip -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d /tmp/gradle
          /tmp/gradle/gradle-8.5/bin/gradle wrapper --gradle-version 8.5

      - name: Build APK
        run: |
          cd ${{ github.workspace }}/signvpn/android
          chmod +x gradlew
          ./gradlew assembleDebug

      - uses: actions/upload-artifact@v4
        with:
          name: SingVPN-HC-APK
          path: ${{ github.workspace }}/signvpn/android/app/build/outputs/apk/debug/*.apk
